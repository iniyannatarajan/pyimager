//# Print revision info for package casaimwrap; generated by CMake

#include <casaimwrap/Package__Version.h>
#include <Common/Version.h>
#include <iostream>

using namespace LOFAR;

int main (int argc, const char* argv[])
{
  std::string type = "brief";
  if (argc > 1) {
    type = argv[1];
  }
  Version::show<casaimwrapVersion> (std::cout, "casaimwrap", type);
  return 0;
}
