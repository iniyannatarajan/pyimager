//# Get revision info for package StationResponse; generated by CMake

#include <StationResponse/Package__Version.h>
#include <Common/Version.h>
#include <Common/Package__Version.h>
#include <ElementResponse/Package__Version.h>

namespace LOFAR {
  Version StationResponseVersion::getInfo()
  {
    return Version ("StationResponse",
                    getVersion(), getConfVersion(),
                    getRevision(), getPackageRevision(),
                    getNrChangedFiles(),
                    getBuildTime(), getBuildUser(), getBuildMachine());
  }

  std::string StationResponseVersion::getVersion()
    { return "CEP-Imager-Task5206"; }
  std::string StationResponseVersion::getConfVersion()
    { return "0.1"; }
  std::string StationResponseVersion::getRevision()
    { return "31269"; }
  std::string StationResponseVersion::getPackageRevision()
    { return "29845"; }
  std::string StationResponseVersion::getNrChangedFiles()
    { return "0"; }
  std::string StationResponseVersion::getBuildTime()
  { return std::string(__DATE__) + " " + std::string(__TIME__); }
  std::string StationResponseVersion::getBuildUser()
  { return "iniyan"; }
  std::string StationResponseVersion::getBuildMachine()
  { return "pyimager.c.meerkat-7-gazing.internal"; }
  void StationResponseVersion::fillInfo
    (int level, std::vector<Version::PkgInfo>& vec)
  {
    // Add info of this package if needed.
    vec.push_back (Version::PkgInfo(level, &getInfo));
    // Add info of packages being used.
    CommonVersion::fillInfo (level+1, vec);
    ElementResponseVersion::fillInfo (level+1, vec);
  }
}
