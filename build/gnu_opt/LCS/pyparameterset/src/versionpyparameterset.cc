//# Print revision info for package pyparameterset; generated by CMake

#include <pyparameterset/Package__Version.h>
#include <Common/Version.h>
#include <iostream>

using namespace LOFAR;

int main (int argc, const char* argv[])
{
  std::string type = "brief";
  if (argc > 1) {
    type = argv[1];
  }
  Version::show<pyparametersetVersion> (std::cout, "pyparameterset", type);
  return 0;
}
